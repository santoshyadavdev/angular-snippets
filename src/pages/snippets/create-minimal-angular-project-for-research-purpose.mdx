---
title: Create minimal Angular project for researching purpose
description: "Simple command line to create an Angular project with bare minimum of files for researching the framework purpose"
tags: ["angular"]
pubDate: Mar 2, 2023
contributedBy: "@HienHuuPham"
---

import BlogImage from "@components/BlogImage.astro";
import MinimalProject from "@images/minimal-angular-project.png";

Do you want to create a new Angular project with bare minimum of files for researching Angular framework?

Just use this command line and you are good to go.

```bash
ng new learn-pipe --minimal --defaults
```

- `--minimal` is the short-hand for `--inline-template --inline-style --skip-tests`
- `--defaults` is the short-hand for `--style=css --router=false`

<BlogImage src={MinimalProject} alt="create minimal angular project" />
